const User = require('../Models/User')

class HomeController {

    getPost (req, res) {
        try {
           
        } 
        catch (error) {
            res.json(error)
        }
    }
}

module.exports = new HomeController
 